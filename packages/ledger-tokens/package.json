{
  "name": "@ash-archive/ledger-tokens",
  "version": "0.1.0",
  "description": "The Ledger System token contract — GENESIS 03 §XI. Three layers (primitive → semantic → component), CSS custom properties + JSON. Wings consume tokens, never raw values.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./tokens.css",
    "./tokens.json": "./tokens.json",
    "./fonts.css": "./fonts/fonts.css"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "@fontsource-variable/crimson-pro": "^5.3.0",
    "@fontsource-variable/ibm-plex-sans": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
