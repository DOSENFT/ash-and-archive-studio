{
  "name": "@ash-archive/atelier",
  "version": "0.1.0",
  "description": "The world layer — SPEC-SH3. Consumer of nothing in core; peer of the composer; the Foundation never knows it exists.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=22.5" },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
